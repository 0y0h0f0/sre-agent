# Phase 8：前端增强（体验优化）

目标：从能用到好用。在现有 React 控制台（见 `06-frontend/react-console.md`）基础上做实时进度、诊断可视化和移动端适配。

## 8.1 实时诊断进度

目标：诊断过程中实时展示当前节点。

| 任务 | 细节 |
| --- | --- |
| WebSocket / SSE | 后端推送 LangGraph 节点状态变化（替代 5 秒轮询） |
| 进度条 | 从后端返回的 graph/node trace 动态计算进度；当前 graph 是 15 个节点，不能在前端硬编码 X/14 |
| 节点日志 | 实时展示当前节点的执行日志和中间输出 |

## 8.2 诊断可视化

目标：更直观地展示诊断链路。

| 任务 | 细节 |
| --- | --- |
| 泳道图 | 展示各数据源的时间线（metrics/logs/traces/git 时间对齐） |
| 依赖图 | 服务拓扑 + 异常节点高亮（用 Cytoscape.js / D3.js） |
| 证据网络 | 假说 ↔ 证据的关联图，展示置信度流向 |

## 8.3 移动端适配

目标：手机审批和查看。

| 任务 | 细节 |
| --- | --- |
| 响应式布局 | 审批页面移动端优先设计 |
| PWA | Service Worker + manifest，可添加到手机桌面 |
| 推送通知 | Web Push API（审批请求到达时推送） |
